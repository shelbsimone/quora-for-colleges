<%= simple_form_for([@question, @comment], html: {class: "form-signin"}) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <!-- f.input :commenter %>-->
    <%= f.input :body %>
    <!-- f.association :question %>-->
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
